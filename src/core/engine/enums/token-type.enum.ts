export enum TokenType {
  OPEN_BRACE,
  CLOSE_BRACE,
  OPEN_IF,
  CLOSE_IF,
  ELSE,
  OPEN_EACH,
  CLOSE_EACH,
  IDENTIFIER,
  OPERATOR,
  LITERAL,
  HTML,
  PARTIAL,
  EOF,
  INTERPOLATION,
  DISCARD,
  ERROR
}
